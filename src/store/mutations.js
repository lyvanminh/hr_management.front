export const CHANELS = (state, chanels) => {
    state.chanels = chanels;
}

export const LANGUAGES = (state, languages) => {
    state.languages = languages;
}

export const LEVELS = (state, levels) => {
    state.levels = levels;
}

export const POSITIONS = (state, positions) => {
    state.positions = positions;
}

export const USERS = (state, users) => {
    state.users = users;
}

export const CANDIDATES = (state, candidates) => {
    state.candidates = candidates;
}
